function evaporator(content, evap_per_day, threshold){ 
 let days = 0;
 let renderedUseless = content / threshold;
 console.log(renderedUseless);
 //create for loop to increase days and divide the content with the evap_per_day until it hits the threshold
}
evaporator(10,10,10);
//should be 22

function maxSequence(arr) {
  if(arr.length === 0 || arr === ) {
    return 0; //should return null if array is empty or only negative variables
  }
  return console.log(arr.map( function(a, b) {return a+b}));
}
maxSequence([1, 2, 3, 4, -5, 6, 7])
maxSequence([]);
maxSequence([-2]);

